# 🎓 Attendance Assistant (School Backoffice Extension)

Це розширення для браузера Chrome, розроблене для автоматизації перевірки відвідуваності учнів на платформі Logika School (Backoffice).

Воно автоматично сканує сторінку групи, знаходить учнів із критичною кількістю пропусків та виводить їх у зручну бічну панель для подальшої обробки.

## ✨ Основні можливості

* **🔍 Автоматичний моніторинг:** Використовує `MutationObserver` для миттєвого реагування на завантаження даних на сторінці.
* **🧠 Розумна логіка пошуку:**
    * Знаходить учнів з 2+ пропусками поспіль (🔴🔴).
    * Враховує ситуацію, коли учень прийшов 1 раз після серії пропусків (🔴🔴🟢 — борг залишається).
    * Ігнорує "майбутні" (сірі) уроки.
* **📋 Зручний SideBar:** Список боржників відображається у плаваючій панелі праворуч.
* **⚡ Швидкі дії:**
    * 👁 **Перегляд:** Скролить сторінку до конкретного учня та підсвічує його рядок.
    * ✅ **Виконано:** Прибирає учня зі списку та зберігає його статус у пам'ять браузера (`localStorage`).
* **💾 Збереження стану:** Розширення пам'ятає, кого ви вже відмітили як "опрацьованого", навіть після перезавантаження сторінки.

## 🛠 Встановлення (Developer Mode)

Оскільки це розширення призначене для внутрішнього використання, воно встановлюється в режимі розробника:

1.  **Завантажте код:**
    Клонуйте цей репозиторій або завантажте ZIP-архів і розпакуйте його.
    ```bash
    git clone [https://github.com/your-username/attendance-assistant.git](https://github.com/your-username/attendance-assistant.git)
    ```
2.  **Відкрийте керування розширеннями:**
    Перейдіть за адресою `chrome://extensions/` у браузері Chrome.
3.  **Увімкніть режим розробника:**
    Активуйте перемикач **"Developer mode"** (Режим розробника) у верхньому правому куті.
4.  **Завантажте розширення:**
    Натисніть кнопку **"Load unpacked"** (Завантажити розпаковане) і виберіть папку з файлами проєкту (`manifest.json`, `content.js` тощо).

## ⚙️ Налаштування

Якщо платформа оновить дизайн або змінить кольори, ви можете легко підправити конфігурацію у файлі `content.js`:

```
const CONFIG = {
    // RGB кольори статусів уроків
    red: "244, 91, 91",   
    green: "149, 193, 31", 
    gray: "242, 242, 242",
    
    // CSS селектори для пошуку елементів
    selectors: {
        grid: 'div[style*="repeat(16, 1fr)"]', // Сітка уроків
        cell: '.ant-flex',                      // Окремий квадратик
        name: ".ant-typography.hover-brand-effect" // Ім'я учня
    }
};
```
## 🚀 Як користуватися
1. Зайдіть у Backoffice на сторінку будь-якої групи.
2. Розширення автоматично просканує таблицю.
3. Якщо знайдено боржників, праворуч з'явиться панель зі списком.
4. Натисніть 👁, щоб знайти учня в таблиці.
5. Натисніть ✅ Виконано, якщо ви зв'язалися з батьками або призначили відпрацювання. Учень зникне зі списку.
6. Щоб повернути прихованих учнів, натисніть кнопку "↺ Скинути" у заголовку панелі.

## 🔒 Приватність
Розширення працює виключно локально у вашому браузері. Воно не відправляє жодних даних на зовнішні сервери. Список "ігнорованих" учнів зберігається у localStorage вашого браузера.

## 📝 Disclaimer
Це неофіційний інструмент. Він базується на DOM-структурі сайту станом на 2025 рік. Будь-які зміни в верстці сайту можуть тимчасово порушити роботу розширення.
